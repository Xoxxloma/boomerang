import { InlineKeyboard, type Bot } from 'grammy';
import { buildDigest } from '../../retrieval/digest.js';
import { startImport } from '../../import/burst.js';
import { env } from '../../config/env.js';

// Текст по пользам (сценариям): каждый блок — «польза → как пользоваться». Сквозная метафора —
// бумеранг («что отправил, вернётся, когда нужно»), обыгрывает имя бота. Перечислены только реально
// работающие фичи (голос/видео/фото названы явно; напоминания и Эхо честно; авто-всплытия нет).
const START_TEXT = [
  '*Boomerang* — что отправил, вернётся, когда понадобится.',
  'Пересылай что угодно — бот сам разберёт и поднимет это в нужный момент. Без тегов, папок и ручной сортировки.',
  '',
  '📥 *Принимает всё — и читает за тебя*',
  'Ссылки и статьи, посты из каналов, картинки (распознаёт текст на фото), голосовые и видео ' +
    '(расшифровывает речь в текст), PDF и Word. Бросил — сразу «Принял ✅», заголовок и смысл бот доберёт сам в фоне.',
  '',
  '🔍 *Находит по смыслу, а не по тегам*',
  'Спроси словами, как вспомнилось, — пусть даже неточно. В ответ соберёт связный текст со ссылками ' +
    'на твои же сохранёнки, а не простыню результатов. Кнопка «🔍 Найти» или `/find`.',
  '',
  '🪃 *Возвращает в момент*',
  'Скажи «напомни через неделю» прямо при пересылке или нажми 🪃 на любой записи — бот сам принесёт её ' +
    'вовремя. Не будильник — бумеранг: вещь сама возвращается к тебе.',
  '',
  '🗺 *Показывает всю картину* — /app',
  'Мини-приложение: карта-созвездие записей (близкое — рядом), «переклички» давнего с недавним и ' +
    'годовщины «в этот день», ближайшие напоминания и тот же умный поиск.',
  '',
  '📂 *Помнит, откуда что* — /folders',
  'Раскладка по источникам: каналы и «загружено вручную».',
  '',
  '*С чего начать — закинь то, что уже накопилось:*',
  '• Жми «Залить из Избранного» ниже и пересылай пачками (до 100 за раз).',
  '• Или выгрузи JSON: Telegram Desktop → «Избранное» → ⋮ → «Экспорт истории чата» → формат *JSON*, ' +
    'без медиа → пришли файл `result.json`, разберу всё разом.',
  '',
  'Команды: /find · /app · /folders · /digest · /import',
].join('\n');

/** Кнопка-CTA на приветственном экране: новичку искать нечего — предлагаем сразу залить старое.
 *  Вход в Mini App — отдельной командой /app и кнопкой на reply-клавиатуре (не дублируем тут). */
const startKeyboard = new InlineKeyboard().text('Залить из Избранного', 'import:start');

export function registerCommands(bot: Bot): void {
  bot.command('start', async (ctx) => {
    await ctx.reply(START_TEXT, { parse_mode: 'Markdown', reply_markup: startKeyboard });
  });

  // Вход в Mini App из списка команд: команда — это текст, открыть вебапп можно только кнопкой,
  // поэтому отвечаем инлайн-кнопкой web_app (один тап → приложение).
  bot.command('app', async (ctx) => {
    await ctx.reply('Boomerang — Карта, Поиск и Эхо в одном окне:', {
      reply_markup: new InlineKeyboard().webApp('🪃 Открыть приложение', env.WEBAPP_URL),
    });
  });

  // Режим массовой заливки: открыть сессию, в которую копятся все пересылки (см. import/burst.ts).
  bot.command('import', async (ctx) => {
    const started = await startImport(ctx.api, ctx.from!.id, ctx.chat.id);
    if (!started) {
      await ctx.reply('Заливка уже идёт — просто пересылай дальше.');
    }
  });

  bot.command('digest', async (ctx) => {
    // Дайджест детерминированный (без LLM, без кнопок) — простой список последней активности.
    const { text } = await buildDigest(ctx.from!.id);
    await ctx.reply(text, {
      parse_mode: 'HTML',
      link_preview_options: { is_disabled: true },
    });
  });
}
